{
  "name": "instagram-clone-tests",
  "version": "1.0.0",
  "description": "Integration smoke-tests for the likes feature",
  "private": true,
  "scripts": {
    "test": "jest --testPathPattern=tests/"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
